#pragma once

/**
 * 打印双目运算结果（格式：a 运算符 b = result）
 * @param a      左操作数
 * @param op     运算符字符（如 '+', '-', '*', '/'）
 * @param b      右操作数
 * @param result 运算结果
 */
void print_result(double a, char op, double b, double result);